1. Use bun package manager
2. Follow SOLID principles
3. Use 2 space for indentation
7. When tests failing with unknows causes add debug logs uning `debug` to calling functions to understand why it's failing 
8. To read github issue use command `gh issue view NNNN`
9. To run in background use &:

```sh
bun dev &
```
